#pragma once
#include "header.h"

#include "header.h"

stack *newStack(int capacity);
int size(stack *pt);
int isEmpty(stack *pt);
int isFull(stack *pt);
void push(stack *pt, node *x);
node* peek(stack *pt);
node* pop(struct stack *pt);
stack *newStack(int capacity);
int size(stack *pt);
int isEmpty(stack *pt);
int isFull(stack *pt);
void push(stack *pt, node *x);
node *peek(stack *pt);
node *pop(struct stack *pt);
